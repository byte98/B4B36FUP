
;;;Function which reverses list
(define (reverse-list lst)                              ;reverse-list(list)
  (if (null? lst)                                       ;list == NULL ?
      '()                                               ;TRUE: return NULL
      (append (reverse-list (cdr lst)) (list (car lst)));FALSE: return reverse-list(list[without first one]).append(list[0])
   )
)